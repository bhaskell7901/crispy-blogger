<div class="comments-section">
    <h4>Comments:</h4>
    <div class="comment">
        <p class="text">{{message}}</p>
        <div class="signature">
            <p>{{account.username}} - {{format_date createdAt}} {{format_time createdAt}}</p>
        </div>
    </div>
</div>
      